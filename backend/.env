# ── Server ───────────────────────────────
PORT=5000

# ── MySQL (XAMPP defaults) ───────────────
DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=root
DB_NAME=uniforms_db

# ── JWT ─────────────────────────────────
# Change these to long random strings in production!
JWT_SECRET=setec_jwt_super_secret_key_2024
JWT_EXPIRES_IN=8h
